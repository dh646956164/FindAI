import { BarChart3, Bot, GitCompareArrows, Lightbulb, Newspaper, PackageSearch } from "lucide-react";
import { AdminShell } from "@/components/admin/admin-shell";
import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function AdminPage(){let counts=[0,0,0,0,0,0];try{counts=await Promise.all([prisma.aiTool.count(),prisma.aiTool.count({where:{supportsAgent:true}}),prisma.ranking.count(),prisma.comparison.count(),prisma.useCaseGuide.count(),prisma.aiNews.count()]);}catch{}const stats=[[PackageSearch,"工具",counts[0],"/admin/tools"],[Bot,"Agent",counts[1],"/admin/agents"],[BarChart3,"榜单",counts[2],"/admin/rankings"],[GitCompareArrows,"对比",counts[3],"/admin/comparisons"],[Lightbulb,"场景",counts[4],"/admin/use-cases"],[Newspaper,"情报",counts[5],"/admin/news"]] as const;return <AdminShell title="管理概览" description="维护工具档案、榜单、场景指南和情报内容。"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{stats.map(([Icon,label,count,href])=><a href={href} key={label} className="rounded-2xl border bg-white p-6 hover:border-primary/30 hover:shadow-sm"><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-xl bg-primary/8 text-primary"><Icon className="size-5"/></span><span className="text-3xl font-black">{count}</span></div><p className="mt-5 text-sm font-bold">{label}内容</p></a>)}</div><div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm leading-7 text-amber-900">后台依赖 PostgreSQL。若统计均为 0 或列表提示数据库不可用，请先配置 DATABASE_URL，再执行 <code className="rounded bg-white px-1.5 py-1">npm run db:push</code> 和 <code className="rounded bg-white px-1.5 py-1">npm run db:seed</code>。</div></AdminShell>;}
